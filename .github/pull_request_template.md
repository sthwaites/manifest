## What changed
<!-- One paragraph describing the change -->

## How it was tested
- [ ] `npm run test -- --run` passes
- [ ] `npm run build` succeeds
- [ ] Feature verified in browser at localhost:3000
- [ ] No secrets in diff (`git diff --staged | grep -E "(sk-|AUTH0_CLIENT_SECRET|NEXTAUTH_SECRET)"`)

## Codex context
<!-- If this PR was opened by Codex, paste the thread ID and the feature request prompt -->
Thread ID:
Feature prompt:

## Screenshots / demo
<!-- Optional: screenshot or short gif showing the feature working -->
